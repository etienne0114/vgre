{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vgre.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "vgre.labels" . | nindent 4 }}
{{- end }}
