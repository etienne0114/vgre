# Do NOT pin gradio here: HF's Gradio SDK injects its own gradio (the version
# from README's sdk_version, else its latest) into the same pip install, so a
# hard pin can collide with it (ResolutionImpossible). The Gradio version is
# selected via `sdk_version` in README.md; app.py is written to work across
# Gradio 4/5/6 regardless. Keep this file to the extra deps HF doesn't provide.
numpy
